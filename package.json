{
  "name": "better-runner",
  "version": "0.0.1",
  "description": "A little project to store running data",
  "main": "index.js",
  "author": "barberaf84@gmail.com",
  "license": "MIT",
  "devDependencies": {
    "@ngneat/falso": "^7.2.0",
    "@types/node": "^22.7.2",
    "typescript": "^5.5.4"
  },
  "scripts": {
    "build": "tsc",
    "start": "yarn build && node build/index.js",
    "test": "yarn build && node --test build/**/*.test.js",
    "coverage": "yarn build && node --test --experimental-test-coverage build/**/*.test.js"
  }
}
